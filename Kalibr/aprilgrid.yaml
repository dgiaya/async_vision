  
target_type: 'aprilgrid'
tagCols: 6     # Number of tags (columns)
tagRows: 9     # Number of tags (rows)
tagSize: 0.034  # Size of one tag in meters    1.5 inch
tagSpacing: 0.29 # ratio: gap/tagSize ; gap = between 2 centers of 2 tags

# cam0:
#   camera_model: pinhole
#   intrinsics: [1000, 1000, 612, 512]  # fx=fy=1000, cx=width/2, cy=height/2
#   distortion_model: radtan
#   distortion_coeffs: [0.0, 0.0, 0.0, 0.0]  # No distortion 
#   resolution: [1224, 1024]
#   rostopic: /cam_sync/cam0/image_raw

# cam1:
#   camera_model: pinhole
#   intrinsics: [1000, 1000, 612, 512] 
#   distortion_model: radtan
#   distortion_coeffs: [0.0, 0.0, 0.0, 0.0]
#   resolution: [1224, 1024]
#   rostopic: /cam_sync/cam1/image_raw
  
  
  
## rosrun kalibr kalibr_calibrate_cameras   --bag /data/merged_payload4b/alternate_skipped_ros1.bag   --target /data/aprilgrid.yaml   --models pinhole-radtan pinhole-radtan   --topics /cam_sync/cam0/image_raw /cam_sync/cam1/image_raw   --export-poses

